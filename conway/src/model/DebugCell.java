package model;

/**
 * used this while getting the logic right (can look at x / y values in debugger)
 */
public class DebugCell extends  ClassicCell {
    final int x;
    final int y;

    public DebugCell(int x, int y) {
        this.x = x;
        this.y = y;
    }
}
